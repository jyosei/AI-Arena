version: "3.9"
volumes:
  db_data:

services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: '123456'
      MYSQL_DATABASE: aiarena
    command:
      - '--character-set-server=utf8mb4'
      - '--collation-server=utf8mb4_unicode_ci'
    ports: ["3306:3306"]
    volumes:
      - db_data:/var/lib/mysql

  backend:
    build: ./backend
    # 不再需要对外暴露端口，因为它只和 frontend 通信
    #ports: ["8000:8000"] 
    depends_on: [db]
    volumes: ["./backend:/app"]
    environment:
      DB_NAME: 'aiarena'
      DB_USER: 'root'
      DB_PASSWORD: '123456'
      DB_HOST: 'db'
      DB_PORT: '3306'
      # 在这里添加您的 API Key
      OPENAI_API_KEY: 'sk-ser2cEa4NSlALdseJwob4oube3Trg0NOLwvXEH7yUAeYy9k9' 
    # 关键修改：
    # 1. 使用 bash -c 来确保脚本由 bash 执行
    # 2. 确保 wait-for-db.sh 脚本在本地 ./backend 文件夹中存在
    command: >
      bash -c "sed -i 's/\r$//' wait-for-db.sh &&
               ./wait-for-db.sh db python manage.py migrate &&
               python manage.py runserver 0.0.0.0:8000"

  frontend: # <-- 🌟 确保这里有正确的缩进，使其成为 services 的子项
    build:
      context: ./frontend
      args:
        # 使用相对路径 /api/，让浏览器中的请求走 nginx 的 /api/ 代理到后端
        VITE_API_BASE: /api/
    ports: ["8000:80"]  # 主机 8000 映射到容器内 Nginx 监听的 80 端口
    # volumes: ["./frontend:/app"]
    depends_on:
      - backend