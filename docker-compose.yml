version: "3.9"
volumes:
  db_data:

services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: '123456'
      MYSQL_DATABASE: aiarena
    command:
      - '--character-set-server=utf8mb4'
      - '--collation-server=utf8mb4_unicode_ci'
    ports: ["3306:3306"]
    volumes:
      - db_data:/var/lib/mysql

  backend:
    build: ./backend
    # 不再需要对外暴露端口，因为它只和 frontend 通信
    #ports: ["8000:8000"] 
    depends_on: [db]
    volumes: ["./backend:/app"]
    environment:
      DB_NAME: 'aiarena'
      DB_USER: 'root'
      DB_PASSWORD: '123456'
      DB_HOST: 'db'
      DB_PORT: '3306'
      # 在这里添加您的 API Key
      OPENAI_API_KEY: 'sk-ser2cEa4NSlALdseJwob4oube3Trg0NOLwvXEH7yUAeYy9k9' 
    # 使用 "sh wait-for-db.sh" 而非直接 ./wait-for-db.sh，规避 Windows CRLF 行尾可能导致的 shebang 解析失败 (/bin/sh\r not found) 问题。
    # 传递的后续命令不再用单引号包裹，确保脚本中 exec 能正确分词并执行 migrate 与 runserver。
  # 运行前对脚本做 CRLF 清洗 (仅运行时，不修改源)，再执行迁移与启动。
  command: sh -c "tr -d '\r' < wait-for-db.sh > /tmp/wdb.sh && sh /tmp/wdb.sh db python manage.py migrate && python manage.py runserver 0.0.0.0:8000"

  frontend: # <-- 🌟 确保这里有正确的缩进，使其成为 services 的子项
    build:
      context: ./frontend
      args:
        # 使用相对路径 /api/，让浏览器中的请求走 nginx 的 /api/ 代理到后端
        VITE_API_BASE: /api/
    ports: ["8000:80"]  # 主机 8000 映射到容器内 Nginx 监听的 80 端口
    # volumes: ["./frontend:/app"]
    depends_on:
      - backend