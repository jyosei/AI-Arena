# AI Arena 系统架构详细文档

> **版本**: 2.0  
> **最后更新**: 2025-12-08  
> **架构风格**: 微服务 + 前后端分离
> **编辑**: shallcheer

完整的系统架构设计、组件交互、数据流和技术决策。

---

## 📑 目录

1. [整体架构](#整体架构)
2. [前端架构](#前端架构)
3. [后端架构](#后端架构)
4. [数据库架构](#数据库架构)
5. [系统交互流](#系统交互流)
6. [技术栈](#技术栈)
7. [部署架构](#部署架构)

---

## 整体架构

### 系统拓扑图

```
┌─────────────────────────────────────────────────────────┐
│                      用户浏览器                          │
└────────────────┬────────────────────────────────────────┘
                 │ HTTPS
                 ▼
┌─────────────────────────────────────────────────────────┐
│              Nginx Reverse Proxy                         │
│  (路由、负载均衡、静态文件服务)                           │
└──────┬──────────────────────────────┬────────────────────┘
       │                              │
       ▼ (前端资源)                   ▼ (API 请求)
┌─────────────────────┐      ┌─────────────────────┐
│   Frontend Server   │      │  Backend API        │
│  (React + Vite)     │      │  (Django REST)      │
│  - 路由处理         │      │  - 认证授权         │
│  - 组件渲染         │      │  - 业务逻辑         │
│  - 状态管理         │      │  - 数据验证         │
└──────────┬──────────┘      └──────┬──────────────┘
           │                        │
           └────────────┬───────────┘
                        │ SQL/WebSocket
                        ▼
            ┌──────────────────────┐
            │   MySQL Database     │
            │  (InnoDB + UTF8MB4)  │
            │  - 数据持久化        │
            │  - 事务管理          │
            │  - 索引优化          │
            └──────────────────────┘
```

### 架构层次

```
┌─────────────────────────────────────────────────┐
│          Presentation Layer (表现层)             │
│  React 组件 + 路由 + 样式 + 用户交互             │
├─────────────────────────────────────────────────┤
│          Communication Layer (通信层)            │
│  HTTP API + WebSocket + 认证 Token              │
├─────────────────────────────────────────────────┤
│          Business Logic Layer (业务逻辑层)       │
│  服务类 + 数据验证 + 业务规则 + 缓存             │
├─────────────────────────────────────────────────┤
│          Data Access Layer (数据访问层)          │
│  ORM 模型 + 数据库查询 + 索引优化                |
├─────────────────────────────────────────────────┤
│          Database Layer (数据库层)               │
│  MySQL 表 + 外键约束 + 事务支持                 │
└─────────────────────────────────────────────────┘
```

---

## 前端架构

### 前端文件结构

```
frontend/
├── public/               # 静态资源
│   ├── test.html
│   └── favicon.ico
├── src/
│   ├── main.jsx         # 应用入口
│   ├── App.jsx          # 根组件
│   ├── global.css       # 全局样式
│   │
│   ├── api/             # API 调用层
│   │   ├── apiClient.js   # 统一 HTTP 客户端
│   │   ├── forum.js       # 论坛 API
│   │   ├── models.js      # 模型 API
│   │   └── ...
│   │
│   ├── components/      # 可复用组件
│   │   ├── Header.jsx
│   │   ├── Footer.jsx
│   │   ├── Sidebar.jsx
│   │   └── ...
│   │
│   ├── contexts/        # 全局状态管理
│   │   ├── AuthContext.jsx
│   │   └── AppContext.jsx
│   │
│   ├── pages/           # 页面组件
│   │   ├── Home.jsx
│   │   ├── Forum/
│   │   ├── Chat/
│   │   ├── Models/
│   │   └── ...
│   │
│   ├── services/        # 业务逻辑服务
│   │   ├── authService.js
│   │   ├── forumService.js
│   │   └── ...
│   │
│   └── utils/           # 工具函数
│       ├── helpers.js
│       ├── validators.js
│       └── ...
│
├── package.json         # 依赖配置
└── vite.config.js       # Vite 配置
```

### 前端数据流

```
User Interaction (用户交互)
         │
         ▼
    React Event Handler
         │
         ▼
    Service Layer (业务逻辑)
         │
         ▼
    API Client (HTTP 请求)
         │
         ▼
    Backend API
         │
         ▼
    Response (响应)
         │
         ▼
    Context/Redux (状态更新)
         │
         ▼
    Component Re-render (组件重渲染)
         │
         ▼
    UI Updated (界面更新)
```

### 前端页面结构

```
App.jsx
├── Router
│   ├── / (Home Page)
│   │   ├── 导航栏
│   │   ├── 轮播图
│   │   ├── 功能概览
│   │   └── 底部
│   │
│   ├── /forum (论坛)
│   │   ├── PostList (帖子列表)
│   │   │   ├── Filter (筛选)
│   │   │   ├── Search (搜索)
│   │   │   └── PostCard x N
│   │   ├── CreatePost (发布帖子)
│   │   └── PostDetail (帖子详情)
│   │       ├── PostContent
│   │       ├── Comments (评论)
│   │       └── CommentForm (评论表单)
│   │
│   ├── /chat (AI 对话)
│   │   ├── ConversationList (对话列表)
│   │   └── ChatWindow (对话窗口)
│   │       ├── MessageList (消息列表)
│   │       └── MessageInput (消息输入)
│   │
│   ├── /models (模型与对战)
│   │   ├── Leaderboard (排行榜)
│   │   ├── ModelList (模型列表)
│   │   └── BattleArena (对战竞技场)
│   │
│   ├── /profile (用户资料)
│   │   ├── ProfileCard
│   │   ├── PostHistory
│   │   └── Settings
│   │
│   ├── /auth (认证)
│   │   ├── Login
│   │   ├── Register
│   │   └── OAuth (GitHub/WeChat)
│   │
│   └── 404 Page
```

### 关键前端组件

| 组件 | 功能 | 状态管理 |
|------|------|--------|
| PostList | 显示帖子列表、分页、筛选 | Context API |
| PostDetail | 显示帖子、评论、互动 | Context API + Local |
| ChatWindow | AI 对话界面 | Context API |
| Leaderboard | 模型排行榜 | Local State |
| UserProfile | 用户信息、设置 | Context API |

---

## 后端架构

### 后端项目结构

```
backend/
├── ai_arena_backend/    # 项目配置
│   ├── settings.py      # Django 设置
│   ├── urls.py          # 根 URL 配置
│   ├── asgi.py          # 异步服务器入口
│   ├── wsgi.py          # WSGI 服务器入口
│   └── __init__.py
│
├── users/               # 用户模块
│   ├── models.py        # User 模型
│   ├── serializers.py   # 用户序列化
│   ├── views.py         # 认证视图
│   ├── urls.py          # 用户路由
│   ├── permissions.py   # 权限类
│   ├── tests.py         # 测试
│   └── migrations/      # 数据库迁移
│
├── forum/               # 论坛模块
│   ├── models.py        # Post, Comment, Category 等模型
│   ├── serializers.py   # 论坛序列化
│   ├── views.py         # 论坛视图 (ViewSet)
│   ├── permissions.py   # 论坛权限
│   ├── urls.py          # 论坛路由
│   ├── tests.py         # 论坛测试
│   ├── management/      # 管理命令
│   │   └── commands/
│   ├── migrations/
│   └── comments/        # 评论历史记录
│
├── models_manager/      # 模型管理模块
│   ├── models.py        # AIModel, 对战, 排行榜等
│   ├── serializers.py   # 模型序列化
│   ├── views.py         # 模型和对战视图
│   ├── services.py      # 业务逻辑服务
│   ├── urls.py          # 路由
│   ├── tests.py         # 测试
│   └── migrations/
│
├── chat/                # AI 对话模块
│   ├── models.py        # Conversation, ChatMessage 等
│   ├── serializers.py
│   ├── views.py
│   ├── urls.py
│   └── tests.py
│
├── datasets/            # 数据集模块
│   ├── models.py
│   ├── views.py
│   └── urls.py
│
├── manage.py            # Django 管理命令
├── requirements.txt     # Python 依赖
├── Dockerfile           # Docker 配置
└── database_health_check.py  # 数据库健康检查
```

### 后端模块架构

```
views.py (API 端点)
    │
    ├─► serializers.py (数据序列化)
    │
    ├─► models.py (数据模型)
    │
    ├─► services.py (业务逻辑)
    │
    ├─► permissions.py (权限控制)
    │
    └─► Database (数据持久化)
```

### Django REST Framework 视图

```python
# ViewSet 架构示例

class PostViewSet(viewsets.ModelViewSet):
    # 查询集
    queryset = ForumPost.objects.all()
    
    # 序列化器
    serializer_class = ForumPostSerializer
    
    # 权限类
    permission_classes = [IsAuthenticatedOrReadOnly]
    
    # 过滤和搜索
    filter_backends = [SearchFilter, OrderingFilter]
    search_fields = ['title', 'content']
    ordering_fields = ['created_at', 'view_count']
    
    # 标准方法
    - list()        # GET /posts/
    - create()      # POST /posts/
    - retrieve()    # GET /posts/{id}/
    - update()      # PUT /posts/{id}/
    - partial_update()  # PATCH /posts/{id}/
    - destroy()     # DELETE /posts/{id}/
    
    # 自定义方法
    @action(detail=True, methods=['post'])
    def like(self, request, pk=None):
        # 点赞逻辑
        pass
```

### API 路由架构

```
/api/
├── /users/
│   ├── POST /register/         # 注册
│   ├── POST /login/            # 登录
│   ├── POST /logout/           # 登出
│   ├── POST /refresh-token/    # 刷新 Token
│   ├── GET /profile/           # 获取个人资料
│   ├── PUT /profile/           # 更新个人资料
│   └── GET /notifications/     # 通知
│
├── /posts/
│   ├── GET /                   # 列表 (支持分类、标签、搜索)
│   ├── POST /                  # 创建帖子
│   ├── GET /{id}/              # 详情
│   ├── PUT /{id}/              # 编辑
│   ├── DELETE /{id}/           # 删除
│   ├── POST /{id}/like/        # 点赞
│   ├── DELETE /{id}/like/      # 取消点赞
│   ├── POST /{id}/favorite/    # 收藏
│   ├── DELETE /{id}/favorite/  # 取消收藏
│   └── GET /{id}/comments/     # 评论列表
│
├── /comments/
│   ├── POST /posts/{id}/comments/     # 发表评论
│   ├── PUT /comments/{id}/            # 编辑评论
│   ├── DELETE /comments/{id}/         # 删除评论
│   ├── POST /comments/{id}/like/      # 点赞评论
│   └── DELETE /comments/{id}/like/    # 取消点赞
│
├── /conversations/
│   ├── GET /                   # 对话列表
│   ├── POST /                  # 创建对话
│   ├── GET /{id}/              # 对话详情
│   ├── DELETE /{id}/           # 删除对话
│   └── POST /{id}/messages/    # 发送消息
│
├── /models/
│   ├── GET /                   # 模型列表
│   ├── GET /{id}/              # 模型详情
│   ├── GET /leaderboard/       # 排行榜
│   └── GET /{id}/test-results/ # 测试结果
│
├── /battles/
│   ├── GET /                   # 对战列表
│   ├── POST /                  # 创建对战
│   ├── POST /{id}/vote/        # 投票
│   └── GET /{id}/results/      # 对战结果
│
└── /datasets/
    ├── GET /                   # 数据集列表
    └── POST /                  # 上传数据集
```

### 后端核心服务

| 服务 | 功能 | 位置 |
|------|------|------|
| AuthService | 认证授权 | users/services.py |
| ForumService | 论坛业务逻辑 | forum/views.py |
| ModelService | 模型管理 | models_manager/services.py |
| BattleService | 对战逻辑 | models_manager/services.py |
| ChatService | 对话处理 | chat/views.py |

---

## 数据库架构

### 数据库设计

**数据库**: MySQL 8.0  
**编码**: utf8mb4  
**存储引擎**: InnoDB  
**事务支持**: 是

### 数据库表组织

```
User Management (用户管理)
├── users_user              # 用户账户
├── users_profile           # 用户资料
└── users_notification      # 用户通知

Forum System (论坛系统)
├── forum_category          # 论坛分类
├── forum_forumpost         # 论坛帖子
├── forum_forumcomment      # 论坛评论
├── forum_tag               # 标签
├── forum_postfavorite      # 帖子收藏
├── forum_postlike          # 帖子点赞
├── forum_commentlike       # 评论点赞
└── forum_postviewhistory   # 帖子浏览历史

Chat System (对话系统)
├── models_manager_chatconversation  # 对话
└── models_manager_chatmessage       # 对话消息

Model Management (模型管理)
├── models_manager_aimodel           # AI 模型
├── models_manager_battle            # 对战记录
├── models_manager_battleresult      # 对战结果
├── models_manager_battlevote        # 对战投票
├── models_manager_leaderboardsnapshot  # 排行榜快照
└── models_manager_modeltestresult   # 模型测试结果

Dataset Management (数据集)
└── datasets_dataset        # 数据集

System (系统)
├── auth_user               # Django 认证用户
├── auth_group              # 用户组
├── auth_permission         # 权限
└── django_*                # Django 系统表
```

### 关键表关系

```
users_user (用户)
├── 1:N → forum_forumpost (发布帖子)
├── 1:N → forum_forumcomment (发表评论)
├── 1:N → models_manager_chatconversation (创建对话)
├── 1:N → models_manager_battle (创建对战)
├── 1:N → forum_postlike (点赞帖子)
└── 1:N → forum_commentlike (点赞评论)

forum_forumpost (帖子)
├── N:1 → users_user (作者)
├── N:1 → forum_category (分类)
├── N:N → forum_tag (标签)
├── 1:N → forum_forumcomment (评论)
├── 1:N → forum_postlike (点赞)
├── 1:N → forum_postfavorite (收藏)
└── 1:N → forum_postviewhistory (浏览历史)

forum_forumcomment (评论)
├── N:1 → users_user (作者)
├── N:1 → forum_forumpost (所属帖子)
├── N:1 → forum_forumcomment (父评论)
├── 1:N → forum_forumcomment (子评论)
└── 1:N → forum_commentlike (点赞)

models_manager_chatconversation (对话)
├── N:1 → users_user (用户)
├── N:1 → models_manager_aimodel (AI 模型)
└── 1:N → models_manager_chatmessage (消息)

models_manager_aimodel (AI 模型)
├── 1:N → models_manager_chatconversation (被用于对话)
├── 1:N → models_manager_battle (作为对战参与者)
└── 1:N → models_manager_modeltestresult (测试结果)
```

### 索引优化

**帖子查询索引** (12 个):
```sql
-- 按作者和创建时间
idx_author_created: (author_id, created_at)

-- 按分类和创建时间
idx_category_created: (category_id, created_at)

-- 按最后活动时间
idx_last_activity: (last_activity_at)

-- 按状态
idx_status: (status)

-- 热门帖子
idx_hot_posts: (status, created_at, view_count)
```

**评论查询索引** (7 个):
```sql
-- 按帖子和创建时间
idx_post_created: (post_id, created_at)

-- 按作者和创建时间
idx_author_created: (author_id, created_at)

-- 按父评论 (树形结构)
idx_parent_created: (parent_id, created_at)
```

---

## 系统交互流

### 用户注册流程

```
1. 用户在前端输入注册信息
   ↓
2. 前端验证输入
   ├─ 用户名格式
   ├─ 邮箱格式
   └─ 密码强度
   ↓
3. 发送 POST /api/users/register/
   ↓
4. 后端接收请求
   ├─ 反序列化数据
   ├─ 验证用户名唯一性
   ├─ 验证邮箱唯一性
   └─ 密码哈希处理
   ↓
5. 创建用户记录
   └─ 保存到数据库
   ↓
6. 返回 200 响应 + 用户信息
   ↓
7. 前端跳转到登录页面
```

### 用户登录流程

```
1. 用户输入邮箱和密码
   ↓
2. 前端发送 POST /api/users/login/
   ↓
3. 后端验证凭证
   ├─ 查询用户
   ├─ 验证密码
   └─ 检查账户状态
   ↓
4. 生成 JWT Token
   ├─ 生成 Access Token (15 分钟有效期)
   ├─ 生成 Refresh Token (7 天有效期)
   └─ 返回两个 Token
   ↓
5. 前端保存 Token
   ├─ Access Token → localStorage
   ├─ Refresh Token → httpOnly Cookie
   └─ 更新应用状态
   ↓
6. 自动跳转到主页
   ↓
7. 后续请求自动携带 Token
   └─ Authorization: Bearer {access_token}
```

### 发布帖子流程

```
1. 用户打开新建帖子页面
   ↓
2. 加载分类和标签列表
   ├─ GET /api/categories/
   └─ GET /api/tags/
   ↓
3. 用户输入帖子内容
   ├─ 标题
   ├─ 内容 (支持 Markdown)
   ├─ 分类
   ├─ 标签
   └─ 图片上传 (可选)
   ↓
4. 前端验证
   ├─ 标题非空 + 长度
   ├─ 内容非空 + 长度
   └─ 至少选择一个分类
   ↓
5. 提交 POST /api/posts/
   ├─ 携带 Authorization 头
   └─ 上传文件 (multipart/form-data)
   ↓
6. 后端处理
   ├─ 验证权限
   ├─ 验证数据
   ├─ 保存帖子
   ├─ 关联分类和标签
   └─ 保存图片
   ↓
7. 返回 201 + 帖子信息
   ↓
8. 前端跳转到帖子详情页
```

### AI 对话流程

```
1. 用户选择 AI 模型
   ↓
2. 创建对话
   POST /api/conversations/
   ├─ model_id: 1
   └─ title: "学习讨论"
   ↓
3. 进入对话页面
   ├─ 加载历史消息
   └─ 显示输入框
   ↓
4. 用户输入消息
   ↓
5. 前端发送消息
   POST /api/conversations/{id}/messages/
   ├─ content: "Python 如何使用列表?"
   └─ 可选: attachments (图片等)
   ↓
6. 后端处理
   ├─ 保存用户消息
   ├─ 调用 AI 模型 API
   ├─ 获取 AI 响应
   └─ 保存 AI 消息
   ↓
7. WebSocket 推送消息
   ├─ 用户消息立即显示
   └─ AI 响应流式返回
   ↓
8. 前端实时更新
   ├─ 显示用户消息
   └─ 流式显示 AI 响应
   ↓
9. 消息保存到数据库
   ├─ models_manager_chatmessage
   └─ 支持查询历史
```

### 数据访问流

```
前端 API 请求
    │
    ▼
Django URL 路由 (urls.py)
    │
    ▼
ViewSet (views.py)
    ├─ 验证权限
    ├─ 验证输入
    │
    ▼
序列化器 (serializers.py)
    ├─ 数据转换
    ├─ 数据验证
    │
    ▼
ORM 模型 (models.py)
    │
    ▼
数据库查询
    ├─ 执行 SQL
    ├─ 使用索引
    │
    ▼
结果集返回
    │
    ▼
序列化响应
    │
    ▼
JSON 响应
    │
    ▼
前端展示
```

---

## 技术栈

### 前端技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 框架 | React 18 | UI 框架 |
| 构建 | Vite | 快速开发和构建 |
| 样式 | Ant Design + CSS | UI 组件库 + 自定义样式 |
| 状态 | Context API | 全局状态管理 |
| 路由 | React Router | 页面路由 |
| HTTP | Axios | API 调用 |
| 国际化 | i18n | 多语言支持 |

**技术决策**:
- ✅ React: 生态好, 易于学习, 组件化
- ✅ Vite: 快速开发体验, 快速冷启动
- ✅ Context API: 轻量级状态管理, 不需要 Redux
- ✅ Ant Design: 专业 UI 组件库

### 后端技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 框架 | Django 5.1 | Web 框架 |
| API | Django REST Framework | RESTful API |
| 认证 | JWT + Django Auth | Token 认证 |
| ORM | Django ORM | 数据库映射 |
| 缓存 | Redis (可选) | 性能缓存 |
| 任务队列 | Celery (可选) | 异步任务 |
| 测试 | pytest + pytest-django | 单元测试 |

**技术决策**:
- ✅ Django: 企业级框架, 功能完整, 安全性高
- ✅ DRF: 标准 RESTful API 实现
- ✅ JWT: 无状态认证, 适合前后端分离
- ✅ ORM: 对象映射, 提高开发效率

### 数据库技术

| 组件 | 技术 | 用途 |
|------|------|------|
| 数据库 | MySQL 8.0 | 关系型数据库 |
| 编码 | UTF8MB4 | Unicode 支持 |
| 存储引擎 | InnoDB | 事务支持 |
| 连接池 | Django Connection Pooling | 连接复用 |
| 备份 | MySQL Backup | 数据备份 |

**技术决策**:
- ✅ MySQL 8.0: 成熟稳定, 性能好
- ✅ InnoDB: 事务支持, 外键约束
- ✅ UTF8MB4: 完整 Unicode 支持

### 部署技术

| 组件 | 技术 | 用途 |
|------|------|------|
| 容器化 | Docker | 应用容器化 |
| 编排 | Docker Compose | 本地开发环境 |
| Web 服务器 | Nginx | 反向代理、负载均衡 |
| 应用服务器 | Gunicorn/uWSGI | WSGI 服务器 |
| 静态文件 | Nginx | 静态文件服务 |

**技术决策**:
- ✅ Docker: 环境一致性, 易于部署
- ✅ Docker Compose: 多容器编排
- ✅ Nginx: 高性能反向代理

---

## 部署架构

### Docker 容器架构

```yaml
Version: 3.9
Services:
  db:
    Image: mysql:8.0
    Ports: 3306:3306
    Environment:
      - MYSQL_DATABASE=aiarena
      - MYSQL_ROOT_PASSWORD=password
    Volumes:
      - mysql_data:/var/lib/mysql
  
  backend:
    Image: backend:latest
    Depends_on: [db]
    Ports: 8000:8000
    Environment:
      - DEBUG=False
      - DATABASE_URL=mysql://user:pass@db:3306/aiarena
    Command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             gunicorn ai_arena_backend.wsgi:application
             --bind 0.0.0.0:8000
             --workers 4"
  
  frontend:
    Image: frontend:latest
    Ports: 3000:80
    Depends_on: [backend]
    Nginx_config: nginx.conf
  
  nginx:
    Image: nginx:latest
    Ports: 80:80, 443:443
    Depends_on: [backend, frontend]
    Volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
```

### 容器网络

```
┌─────────────────────────────────────┐
│        Docker Network                │
├─────────────────────────────────────┤
│                                      │
│  ┌──────────┐  ┌──────────┐         │
│  │Nginx     │  │Frontend  │         │
│  │Port 80   │  │Port 3000 │         │
│  └────┬─────┘  └────┬─────┘         │
│       │             │                │
│       └─────┬───────┘                │
│             │                        │
│       ┌─────▼──────┐                │
│       │  Backend   │                │
│       │  Port 8000 │                │
│       └─────┬──────┘                │
│             │                        │
│       ┌─────▼──────┐                │
│       │  MySQL     │                │
│       │  Port 3306 │                │
│       └────────────┘                │
│                                      │
└─────────────────────────────────────┘
```

### 部署流程

```
1. 构建镜像
   ├─ docker build -t backend:latest ./backend
   └─ docker build -t frontend:latest ./frontend

2. 启动容器
   └─ docker-compose up -d

3. 初始化数据库
   ├─ python manage.py migrate
   ├─ python manage.py createsuperuser
   └─ python manage.py loaddata initial_data

4. 健康检查
   ├─ Database connectivity
   ├─ API availability
   └─ Frontend loading

5. 生产就绪
   ├─ 启用 SSL/TLS
   ├─ 配置日志
   └─ 监控告警
```

### 高可用性考虑

**目前状态**: 单机部署

**未来扩展**:
1. **多实例**
   - 负载均衡 (Nginx/HAProxy)
   - 多个 Backend 实例
   - 数据库主从复制

2. **缓存层**
   - Redis 缓存
   - 热点数据预热
   - 减少数据库压力

3. **异步处理**
   - Celery + Redis
   - 后台任务队列
   - AI 模型调用异步化

4. **监控日志**
   - ELK Stack (Elasticsearch, Logstash, Kibana)
   - Prometheus + Grafana
   - 性能指标收集

---

**版本**: 2.0  
**最后更新**: 2025-12-08  
**架构模式**: 三层架构 (展现层 + 应用层 + 数据层)  
**部署方式**: Docker Compose (本地) / Kubernetes (生产)  
**项目状态**: ✅ ARCHITECTURE COMPLETE
